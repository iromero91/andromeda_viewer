#ifndef ACTION_KEYS_H
#define ACTION_KEYS_H

/**
 * @brief The ACTION_KEY enum defines the various (invertible) actions that can be performed
 * Invertible actions can be "compressed" only if they have the same KEY value.
 *
 * e.g. moving
 */
enum ACTION_KEY : int
{

};

#endif // ACTION_KEYS_H

